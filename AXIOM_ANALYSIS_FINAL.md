# Итоговый анализ аксиоматики проекта P≠NP (Lean 4)

**Дата обновления**: 2025-12-26

---

## 1. Количество аксиом и структура

- **Всего активных аксиом**: 4 (placeholder‑аксиомы в multi‑switching encoding).
- **Распределение**:
  - Part A (Switching/Shrinkage): 0
  - Part A' (Multi-switching encoding placeholders): 4
  - Part D (Magnification): 0
- **Интерфейсы** (`P_subset_Ppoly`, `P_ne_NP_of_nonuniform_separation`):
  реализованы как теоремы, аксиом нет.
- **Архив** (`archive/`, `old_attempts/`): содержит исторические аксиомы, не
  используемые в актуальной сборке.

---

## 2. Проверка на placeholders

- Команда `find pnp3 -name "*.lean" -exec grep -l "sorry\|admit" {} \;` → **пусто**.
- В дереве есть **явные placeholder‑аксиомы** для будущего canonical trace encoding
  (см. `pnp3/AC0/MultiSwitching/Encoding.lean`). Это осознанные заглушки и
  они НЕ используются в текущей цепочке вывода `P_ne_NP_final`.

---

## 3. Классификация по критичности

| Блок | Аксиомы | Роль | Критичность |
|------|---------|------|-------------|
| Part A | — | Связь SAL ↔ switching | 🔴 Критично |
| Part A' | 4 | Заглушка canonical trace encoding | 🟡 В разработке |
| Part D | — | Переход от GapMCSP к `NP_not_subset_Ppoly` (все триггеры доказаны) | 🟢 Доказано |

В активной ветке есть 4 placeholder‑аксиомы в multi‑switching encoding.
Ключевые shrinkage-леммы
`partial_shrinkage_for_AC0` и `shrinkage_for_localCircuit` формально доказаны.
Они параметризованы явными свидетельствами (`AC0CircuitWitness` и
`LocalCircuitWitness`) через `FamilyIsAC0`/`FamilyIsLocalCircuit`, то есть
их статус — теоремы с входными данными, а не внешние аксиомы.

---

## 4. Состояние документации

| Документ | Статус | Комментарий |
|----------|--------|-------------|
| `pnp3/Docs/AXIOMS.md` | ✅ Актуален | Добавлены multi‑switching placeholder‑аксиомы |
| `AXIOMS_FINAL_LIST.md` | ✅ Актуален | Обновлённый список с multi‑switching placeholders |
| `AXIOM_FEASIBILITY_ANALYSIS.md` | ✅ Актуален | Пересчитаны трудозатраты для внешних свидетелей |

---

## 5. История изменений

| Метрика | До архивации | После очистки | Текущее состояние |
|---------|--------------|---------------|-------------------|
| Активные аксиомы | 20 | 16 | **4** |
| Дубликаты в интерфейсе | 2 | 1 | **0** |
| Файлы с `sorry` | 2 | 0 | **0** |

**Примечание**: `partial_shrinkage_for_AC0` и `shrinkage_for_localCircuit`
переведены в статус теорем; обе используют параметризацию свидетельствами,
явно передаваемыми через гипотезы `FamilyIsAC0`/`FamilyIsLocalCircuit`.

---

## 6. Выводы

1. В актуальной ветке есть **4 placeholder‑аксиомы** (multi‑switching encoding),
   но основная цепочка `P_ne_NP_final` ими не пользуется. Ключевые shrinkage-леммы
   формализованы как теоремы и получают свидетелей через параметры.
2. **Интерфейсные утверждения** импортированы как доказанные теоремы — зависимость
   от дополнительных аксиом отсутствует.
3. **Формализация полностью проверяемая**: нет `sorry`, все файлы компилируются.
4. **Дальнейшая работа** должна сосредоточиться на:
   - замене placeholder‑аксиом в `AC0/MultiSwitching/Encoding.lean` на
     конструктивные определения/леммы,
   - построении конкретных shrinkage‑свидетелей для
     `partial_shrinkage_for_AC0` и `shrinkage_for_localCircuit`
     (чтобы доказательства стали автономными).

---

## 7. Рекомендации

- Поддерживать синхронизацию документации при каждом изменении статуса аксиом.
- При планировании больших проектов использовать обновлённый анализ трудозатрат
  из `AXIOM_FEASIBILITY_ANALYSIS.md`.
- Зафиксировать в отдельном отчёте статус архивных аксиом (для исторической
  прозрачности).
