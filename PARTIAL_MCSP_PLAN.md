# План перехода на Partial MCSP (частичные функции)

> **Назначение документа:** дать формальную, воспроизводимую дорожную карту
> миграции от тотальных таблиц истинности к частичным, устранить барьер локальности
> и подготовить последующие доказательства (anti-checker, magnification).
>
> **Важно:** план ориентирован на Lean-код в `pnp3/`, а также на корректное
> согласование с существующей архитектурой проекта.

---

## 0) Анализ текущей архитектуры и источников барьера локальности

**Что есть сейчас:**
- Полные таблицы истинности представлены как `Core.BitVec` длины `2^n`.
- Рестрикции (`Restriction n`) применяются к функциям, но результат часто
  не является корректной таблицей истинности размера `2^n` (или вообще функции на `n` переменных).
- Из-за этого в доказательствах приходится кодировать «урезанные» функции вне модели.

**Что ломается (encoding wall):**
- После применения случайного ограничения к полной таблице истинности
  тип результата становится «вне модели», что ведёт к неаккуратным мостам в доказательствах.
- Это влияет на switching lemma / shrinkage, анти-чекер и магнификацию.

**Вывод:** необходим тип частичной функции, замкнутый относительно рестрикций.

---

## 1) Модель Partial MCSP (Model Refactoring)

### 1.1. Новые типы (каноническое представление)
**Цель:** заменить `Core.BitVec` на частичную функцию, допускающую неопределённые значения,
и сразу выбрать каноническое представление, удобное для рестрикций.

```lean
/-- Canonical representation: a partial function on 2^n inputs. -/
def PartialFunction (n : Nat) := Fin (2^n) → Option Bool

/-- Optional alias for readability; no Vector representation in the first release. -/
def PartialTruthTable (n : Nat) := PartialFunction n
```

### 1.2. Параметры задачи
```lean
structure GapPartialMCSPParams where
  n      : Nat
  s_yes  : Nat
  s_no   : Nat
  gap_ok : s_yes + 1 ≤ s_no
  n_large : 8 ≤ n
  deriving Repr
```

### 1.3. Согласованность схемы с частичной функцией
```lean
/-- Circuit C is consistent with partial truth table T. -/
def is_consistent {n : Nat} (C : Circuit n) (T : PartialTruthTable n) : Prop :=
  ∀ (x : Fin (2^n)),
    match T x with
    | some b => C.eval x = b
    | none   => True
```

### 1.4. Предикаты YES/NO
```lean
def PartialMCSP_YES (p : GapPartialMCSPParams) (T : PartialTruthTable p.n) : Prop :=
  ∃ (C : Circuit p.n), C.size ≤ p.s_yes ∧ is_consistent C T

def PartialMCSP_NO (p : GapPartialMCSPParams) (T : PartialTruthTable p.n) : Prop :=
  ∀ (C : Circuit p.n), is_consistent C T → C.size ≥ p.s_no
```

### 1.5. Определение языка и кодирование входа
**Формат входа (обязательный):** `BitVec (2 * 2^n)` = `mask ++ values`.

- `mask[i] = 1` означает, что значение определено.
- `values[i]` используется **только** когда `mask[i] = 1`.

Это даёт простую декодировку `Option Bool` без «мертвых» кодов.

```lean
def tableLen (n : Nat) : Nat := 2^n
def inputLen (n : Nat) : Nat := 2 * tableLen n  -- фактически `Partial.inputLen`

def maskPart (x : BitVec (inputLen n)) : BitVec (tableLen n) := -- первые N бит
def valPart  (x : BitVec (inputLen n)) : BitVec (tableLen n) := -- вторые N бит

def decodePartial (x : BitVec (inputLen n)) : PartialFunction n :=
  fun i => if maskPart x i = true then some (valPart x i) else none
```

`gapPartialMCSP_Language` должен использовать `decodePartial` как единственный
канонический способ интерпретации входа.

**Целевой файл:** `pnp3/Models/Model_PartialMCSP.lean`.

---

## 2) Рестрикции и замкнутость (Seamless Restriction)

### 2.1. Интуиция
Для **табличных рестрикций** (те, что нужны для снятия барьера локальности)
мы **не уменьшаем `n`**: рестрикция фиксирует часть входных битов кодировки,
и результат остаётся экземпляром `PartialTruthTable n` с большим числом `none`.

Отдельно можно рассмотреть режим «фиксации переменных функции»,
где `n` уменьшается. Это **другая** операция и должна быть оформлена отдельно.

### 2.2. Лемма замкнутости
```lean
theorem restriction_preserves_type
    (T : PartialTruthTable n) (ρ : Restriction (inputLen n)) :
  ∃ (T' : PartialTruthTable n),
      (PartialMCSP_YES p T ↔ PartialMCSP_YES p T') ∧
      (PartialMCSP_NO  p T ↔ PartialMCSP_NO  p T')
```

**Где разместить:** `pnp3/Models/PartialTruthTable.lean`
(модельные леммы про данные и их декодирование).

### 2.3. Практика (обязательные правила)
- **Не аксиоматизировать** `restriction_preserves_type`. Это базовая типовая лемма,
  её следует доказать.
- Построить `T'` явно, с проекцией на свободные координаты.
- Дать аккуратный комментарий, почему доказательство прямое и не требует внешних допущений.

### 2.4. Где реализовать рестрикции частичных таблиц
**Новый файл (рекомендуется):** `pnp3/Models/PartialTruthTable.lean`.

Содержимое:
- `decodePartial` и утилиты `tableLen`, `inputLen`, `maskPart`, `valPart`;
- операции `forget`, `setDefined`, `merge` (минимальный набор);
- функция применения табличной рестрикции к входу;
- леммы о корректности (например, `restriction_preserves_type`).

### 2.5. Минимальная утилита для подстановки входов схем
Даже если основной режим рестрикций работает на уровне таблицы,
нужна лёгкая утилита для работы со схемами:

- `restrictInput` (композиция присваивания с рестрикцией);
- `restrictCircuit` (или лемма об `eval` после подстановки).

Это упростит доказательства согласованности при рестрикциях.

---

## 3) Адаптация Anti-Checker для Partial MCSP

### 3.1. Главная цель
Показать существование частичной функции `T*`, с которой **не согласована**
ни одна малая схема.

### 3.2. Рандомная частичная функция
Для первого релиза фиксируем **α = 1/2**:
- с вероятностью 1/2 ставим `some 0/1`,
- иначе `none`.

### 3.3. Ключевая лемма (без measure-theory)
В Lean удобнее **избежать вероятностей** и работать через подсчёт:
1. оценить число частичных таблиц, согласованных с фиксированной малой схемой;
2. умножить на число малых схем (union bound в форме подсчёта);
3. показать, что это меньше общего числа таблиц при α = 1/2.

Итог: существует `T*`, не согласованная ни с одной малой схемой.

### 3.4. Где менять (параллельный трек)
**Новый файл:** `pnp3/LowerBounds/AntiChecker_Partial.lean`.

- сохранить старый `AntiChecker.lean` как legacy;
- ввести `SmallAC0Solver_Partial` отдельно;
- переносить proof chain на Partial-версию постепенно.

### 3.5. Текущее состояние (выполнено/осталось)
- ✅ Введены `SmallAC0Solver_Partial` и комбинаторные леммы подсчёта.
- ✅ Добавлены partial‑версии `noSmallAC0Solver`, `antiChecker_exists_large_Y`
  и `antiChecker_exists_testset`.
- ✅ Добавлено ядро шага C: `pnp3/LowerBounds/LB_Formulas_Core_Partial.lean`.
- ✅ Добавлены формулировки шага C в магнификационном формате:
  `pnp3/Magnification/PipelineStatements_Partial.lean`.
- 🔜 Осталось связать этот трек с магнификационным конвейером (см. шаг 5).

---

## 4) NP-трудность Partial MCSP (аксиома P/poly locality bridge)

### 4.1. Смысл
Нужна внешняя аксиома: Partial MCSP NP-hard под рандомизированными редукциями.

### 4.2. Новый файл
`pnp3/ThirdPartyFacts/PpolyFormula.lean`
```lean
axiom ppoly_circuit_locality :
  ∃ (p : GapPartialMCSPParams), Is_NP_Hard (gapPartialMCSP_Language p)
```

### 4.3. Использование
В `Magnification/FinalResult.lean` заменить старую гипотезу на новую.

---

## 5) Интеграция и обновление цепочки вывода

- Обновить места, где используются:
  - `Core.BitVec` (теперь `PartialTruthTable`),
  - `hasSmallCircuit`/`onlyLargeCircuits` (теперь `PartialMCSP_YES/NO`),
  - `gapMCSP_Language` (теперь `gapPartialMCSP_Language`).
- **Важно:** модель `Model_GapMCSP.lean` живёт в архиве (`archive/pnp3/Models/`);
  основная ветка ориентирована на Partial MCSP.
  параллельно минимум один релиз.
- Убедиться, что proof chain компилируется без подмены типов через внешние кодировки.

### 5.1. Текущее состояние (выполнено/осталось)
- ✅ Есть partial‑ядро шага C (`LB_Formulas_Core_Partial.lean`).
- ✅ Есть мост Gap→Partial для тотальных таблиц (`Model_PartialMCSP.lean`).
- ✅ Добавлена partial‑версия `PipelineStatements`.
- ✅ Добавлена partial‑версия `Bridge_to_Magnification`
  (`pnp3/Magnification/Bridge_to_Magnification_Partial.lean`) и wiring
  в `FinalResult` (основной финальный вывод теперь partial).
- ✅ Портирован locality‑lift и снят временный аксиоматический мост
  (удалён `ThirdPartyFacts.PartialMagnification.lean`).

---

## 6) Риски и контрольные точки качества

- **Риск:** несогласованная трактовка входа.
  - **Мера:** использовать фиксированную схему `mask ++ values` (см. §1.5) и
    единственную функцию `decodePartial`.
- **Риск:** корректность рестрикций при уменьшении размерности.
  - **Мера:** явно зафиксировать соответствие индексов и доказать консистентность.
- **Риск:** анти-чекер «ослабеет» из-за `none`.
  - **Мера:** фиксировать α = 1/2 и
    доказать экспоненциальную несогласованность.

---

## 7) Список вопросов (максимум полезных для решения)

### Архитектура и типы
1. Где уже используется `Core.BitVec` как модель таблицы истинности? Есть ли
   централизованный типовой alias, чтобы заменить его без каскада правок?
2. Подтверждаем ли каноничность `PartialFunction` и алиас `PartialTruthTable`
   без `Vector`-представления в первом релизе?
3. Как именно `Circuit.eval` ожидает входы: `Fin n → Bool` или `Vector Bool n`?
4. Есть ли уже существующий тип для «три-значной» логики в проекте?

### Кодировка входа
5. Есть ли в `gapMCSP_Language` зафиксированный способ декодирования `BitVec`,
   чтобы унифицировать стиль `decodePartial`?
6. Нужно ли дополнительно документировать «mask ++ values» в README/FAQ,
   или достаточно спецификации в модели?
7. Где в проекте определён тип `Language` и как он интерпретирует входной массив?

### Рестрикции
8. Как именно задан `Restriction n`? Есть ли операции, которые возвращают
   «суженную» функцию или только применяют к схемам?
9. Есть ли готовая функция, которая «подставляет» фиксированные значения
   в схему и уменьшает её по числу переменных?
10. Есть ли уже доказательства о `fixedCount` и корректности индексации после
    удаления фиксированных координат?

### Anti-checker
11. В `AntiChecker.lean` есть общий каркас для «случайной функции»? Можно ли
    расширить его до частичных функций без полной переписки?
12. Какие именно леммы переключения (switching) используются — одиночные,
    multi-switching, или композиция? Нужно ли менять их сигнатуры?
13. Есть ли в проекте уже probabilistic combinatorics (e.g. `Probability`)?
14. В каких местах `AntiChecker` опирается на строгое равенство функций
    (а не на согласованность)? Нужно их идентифицировать.

### NP-hardness аксиома
15. В каких файлах сейчас объявляется NP-hardness MCSP? Какие импорты
    следует продублировать для новой аксиомы?
16. Есть ли в проекте общий стиль для «third-party facts» (комментарии,
    ссылки на статьи, тег `axiom`)?

### Интеграция
17. В каких файлах конечное утверждение `P ≠ NP` использует конкретный язык MCSP?
18. Как организованы импорты моделей в `Magnification/FinalResult.lean`?
19. Нужны ли новые промежуточные леммы о «переносе» между `PartialMCSP` и
    обычной `MCSP` (для сравнения и sanity checks)?
20. Где лучше зафиксировать «ослабление» условия согласованности (чтобы
    различия были прозрачны на уровне доказательств)?

### Верификация и тесты
21. Есть ли набор команд/скриптов, которыми обычно проверяют корректность
    модификаций в `pnp3` (например, `lake build` конкретного модуля)?
22. Встроена ли CI-проверка на отсутствие неиспользуемых аксиом?
23. Нужно ли обновить `AXIOMS_FINAL_LIST.md` после добавления P/poly locality bridge?

---

## 8) Обновлённый ToDo (high level)

- [ ] Добавить `pnp3/Models/PartialTruthTable.lean` (decode, mask/values, операции).
- [ ] Добавить `pnp3/Models/Model_PartialMCSP.lean` с типами и определениями.
- [ ] Реализовать `gapPartialMCSP_Language` на основе `decodePartial`.
- [ ] Доказать лемму `restriction_preserves_type` для Partial MCSP (без аксиом).
- [ ] Добавить `pnp3/LowerBounds/AntiChecker_Partial.lean` и `SmallAC0Solver_Partial`.
- [ ] Внести аксиому `ppoly_circuit_locality` (P/poly locality bridge).
- [ ] Переподключить финальный вывод `P ≠ NP` к новой аксиоме.
- [ ] Обновить список аксиом в `AXIOMS_FINAL_LIST.md` и документы аудита.

---

## 9) Ожидаемый результат

- Устранение барьера локальности за счёт частичных функций.
- Корректная работа рестрикций внутри модели.
- Обновлённые доказательства anti-checker и magnification.
- Опора на конкретную (пусть и аксиоматизированную) NP-hardness Partial MCSP.
