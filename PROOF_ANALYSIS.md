# Анализ конструктивности доказательства P≠NP
## Дата: 2025-12-16

---

## ✅ ЧТО **НЕ** ЯВЛЯЕТСЯ ПРОБЛЕМОЙ

### 1. Classical Logic (классическая логика)
**Статус**: ✅ ПОЛНОСТЬЮ ПРИЕМЛЕМО

- Lean-проекты используют `open Classical` и выбор через `Classical.choose`; это стандарт и не снижает строгость.
- Все современные результаты по нижним оценкам схем строятся в классической логике, поэтому контрапозиции и `by_contra` органичны для доказательства шага D.

### 2. Noncomputable definitions
**Статус**: ✅ ПРИЕМЛЕМО

- Обёртки малых решателей (`SmallAC0Solver`, `SmallSparseSolver` и др.) не требуют вычислимых свидетелей, лишь параметров размера/глубины.
- `noncomputable def` встречается только в вспомогательных конструкциях (например, извлечение схем из `P/poly`) и не влияет на корректность основных теорем.

---

## ⚠️ КРИТИЧЕСКИЕ ФАКТОРЫ

### 1. 🔴 Активные аксиомы

Текущий инвентарь насчитывает **5** внешних аксиом, все из Parts A/C:
- Switching/shrinkage (2): `partial_shrinkage_for_AC0`, `shrinkage_for_localCircuit`.
- Anti-checker (3): `antiChecker_exists_large_Y`, `antiChecker_exists_large_Y_local`, `antiChecker_exists_testset_local`.
- `antiChecker_exists_large_Y` теперь доказана из усиленной аксиомы с тестовым множеством.

Part D не содержит активных аксиом: `OPS_trigger_general`, `OPS_trigger_formulas`, `Locality_trigger` и `CJW_sparse_trigger` доказаны.

### 2. Барьеры доказательства P≠NP

- **Relativization / Algebrization**: использованные техники (switching, античекер, магнификация) являются нон-релятивизующими и не алгебризуются, что соответствует литературе. Формальное обоснование барьеров остаётся задачей статьи, но не Lean-кода.
- **Natural Proofs**: античекер и SAL-атлас дают «ненатуральные» свойства; дополнительный письменный анализ желателен, но контрпримеров в коде нет.

### 3. Peer Review

- Требуется внешняя экспертиза литературы (особенно OPS/CJW) для окончательной валидации. Код компилируется и фиксирует точное место применения каждой аксиомы.

---

## 📊 ТЕКУЩИЙ СТАТУС ДОКАЗАТЕЛЬСТВА

### Доказанные части (Lean 4)
- Part D: все триггеры и мосты `Bridge_to_Magnification` завершены.
- Part B: ёмкостные леммы (`approxOnTestsetWitness_injective` и следствия) доказаны.
- Инфраструктура: интерфейсы `P_subset_Ppoly_proof` и `P_ne_NP_of_nonuniform_separation` импортированы как теоремы, без новых аксиом.

### Частично опирается на аксиомы
- Part A: switching/shrinkage (2 аксиомы).
- Part C: античекер (3 аксиомы), используемые в `LB_Formulas_core` и `LB_LocalCircuits_core`.

---

## 🎯 КЛЮЧЕВЫЕ НАБЛЮДЕНИЯ ПО ШАГУ D

1. **CJW_sparse_trigger доказан**: контрапозиция строится через явный `defaultSparseSolver`, который нарушает гипотезу `SparseLowerBoundHypothesis` при допущении `NP ⊆ P/poly`.
2. **OPS-триггеры конструктивны**: `generalCircuitSolver_of_Ppoly` извлекает неуниформный решатель из интерфейса `Ppoly`, после чего нижняя граница шага C даёт противоречие.
3. **Locality_trigger завершён**: использует `locality_lift` в виде теоремы; дополнительных аксиом в блоке D не возникает.
4. **Мосты шага D замкнуты**: `Bridge_to_Magnification.lean` комбинирует доказанные триггеры с пакетами шага C; незакрытых «statement := Prop» не осталось.

---

## 🗺️ ДОРОЖНАЯ КАРТА

- **Фокус**: устранение 5 аксиом Parts A/C (switching и античекер). Шаг D дополнительных задач не содержит.
- **Документация**: продолжать синхронизировать отчёты с текущим статусом (актуальный инвентарь = 5 аксиом, дата 2025-12-16).
