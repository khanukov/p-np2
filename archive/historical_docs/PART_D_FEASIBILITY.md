# Можно ли Конструктивно Доказать Part D (Magnification)?
## Честная оценка: что мешает и что можно сделать

> ℹ️ **Примечание (исторический документ):** файл написан в контексте
> legacy‑цепочки GapMCSP. Текущий активный конвейер использует Partial MCSP,
> а GapMCSP‑ветка хранится в `archive/`. Ниже сохранены исходные рассуждения
> для исторической прозрачности.

**Дата**: 2025-12-16
**Вопрос**: Есть ли проблема конструктивно доказать Part D, если допускается classical?

---

## 🎯 КРАТКИЙ ОТВЕТ

**Проблема НЕ в classical vs constructive logic** ✅

**Infrastructure теперь достаточна** ✅ (все триггеры формализованы)

**С текущей архитектурой**: Part D полностью доказан в Lean; активных аксиом в блоке D нет. Остаётся 5 внешних предпосылок из Parts A/C.

---

## 📊 ЧТО ТАКОЕ PART D (MAGNIFICATION)?

### 0 Axioms (current status)

- `OPS_trigger_general` — доказано
- `OPS_trigger_formulas` — доказано как специализация D.1
- `Locality_trigger` — доказано
- `CJW_sparse_trigger` — доказано (разреженный язык)
- `locality_lift` — импортируется как внешний факт CJW'22, но оформлен как теорема и не входит в активный список аксиом

**Текущий прогресс (2025-12-16)**:

- ✅ **D.1**: Контрапозиция OPS реализована конструктивно через `generalCircuitSolver_of_Ppoly` и нижнюю границу `LB_GeneralFromLocal`.
- ✅ **D.2**: Формульный триггер (`OPS_trigger_formulas`) доказан как специализация общего случая.
- ✅ **D.3**: `Locality_trigger` выводит `NP \nsubseteq P/poly` из барьера локальности.
- ✅ **D.4**: `CJW_sparse_trigger` заменяет прежнюю аксиому доказательством с явным малым решателем `defaultSparseSolver`.
- ℹ️ Единственные внешние факты, от которых зависит Part D, находятся в частях A/C (switching и античекер); в самом шаге D активных аксиом нет.

### Пример (D.2 — простая специализация)

```lean
theorem OPS_trigger_formulas_from_general
  {p : GapMCSPParams} {δ : Rat} :
  GeneralLowerBoundHypothesis p δ (∀ _ : SmallAC0Solver p, False) →
    NP_not_subset_Ppoly :=
by
  intro h
  exact OPS_trigger_general (p := p) (ε := δ)
    (statement := ∀ _ : SmallAC0Solver p, False) h

-- Итоговый триггер, вызываемый в пайплайне:
theorem OPS_trigger_formulas
  {p : GapMCSPParams} {δ : Rat} :
  FormulaLowerBoundHypothesis p δ → NP_not_subset_Ppoly :=
by
  intro h
  exact OPS_trigger_formulas_from_general (p := p) (δ := δ)
    (FormulaLowerBoundHypothesis.as_general (p := p) (δ := δ) h)
```

**Смысл**: если δ > 0 и нет малых AC⁰-решателей для GapMCSP, то `NP \nsubseteq P/poly`.

---

## 🔍 ПОЧЕМУ ЭТО РАБОТАЕТ СЕЙЧАС

### Infrastructure

- Интерфейсы `NP_not_subset_Ppoly` и `P_ne_NP_of_nonuniform_separation` импортированы как теоремы (`Facts/PsubsetPpoly`), поэтому в Part D нет абстрактных Props.
- Оболочки малых решателей (`SmallAC0Solver`, `SmallGeneralCircuitSolver`, `SmallLocalCircuitSolver`, `SmallSparseSolver`) предоставляют требуемые свидетели для контрапозиций.
- Гипотезы шага C (`LB_Formulas_core`, `LB_LocalCircuits_core`) формируют нужные `statement` без дополнительных предположений.

### Classical Logic

- Все доказательства используют `open Classical` и выбор через `Classical.choose`; это согласовано с остальной библиотекой и не создаёт методологических рисков.

### Оставшиеся внешние зависимости

- 6 активных аксиом лежат в частях A/C (switching и античекеры). Part D лишь потребляет их результаты, но сам не добавляет новых аксиом.

---

## 🛠️ ЧТО ЕЩЁ МОЖНО УЛУЧШИТЬ

- Упростить константы по умолчанию (`opsDefaultPack`) при появлении более острых оценок, хотя это не критично для доказательства.
- Добавить более детальные связи с опубликованными параметрами CJW/OPS (например, численные барьеры для `polylogBudget`) в комментариях, чтобы облегчить навигацию ревьюерам.

---

## 📝 ИТОГОВЫЕ ВЫВОДЫ

- Part D (magnification) целиком формализован в Lean 4 и не содержит активных аксиом.
- Все триггеры (`OPS`, `Locality`, `CJW_sparse`) доступны как теоремы и используются в мостах шага D (`Bridge_to_Magnification.lean`).
- Дальнейшая работа по устранению аксиом сфокусирована на частях A/C; для шага D дополнительных задач нет.
