/--
# BooleanBasics

Базовые определения, которые понадобятся для построения Switching-Atlas Lemma (SAL).

*Опорные задачи*:
- определить подкуб пространства `Bool^n` как отображение `Fin n → Option Bool`;
- описать принадлежность точки подкубу и операцию «точки, совместимые с подкубом»;
- ввести равномерное распределение `U_n` и функцию ошибки аппроксимации `err_U`.

Этот файл пока содержит только спецификации и комментарии; реализация появится в ходе шага A.
-/

namespace Pnp3

/-- TODO: определить подкуб как `Fin n → Option Bool` и реализовать базовые операции. -/
axiom Subcube (n : ℕ) : Type

/-- TODO: определить принадлежность точки подкубу. -/
axiom mem {n : ℕ} : Subcube n → (Fin n → Bool) → Prop

/-- TODO: равномерная мера на `Bool^n` и ошибка аппроксимации. -/
axiom err_U {n : ℕ}
  (f : Fin n → Bool → Bool)
  (Rset : List (Subcube n)) : ℚ

end Pnp3
