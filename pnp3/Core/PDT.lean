/--
# PDT (Partial Decision Trees)

Шаблон для определения частичных решающих деревьев, которые будут использоваться для получения общего атласа.

*Планируемые компоненты*:
- структура для PDT с узлами и листьями-подкубами;
- функция `leaves`, возвращающая список подкубов;
- инварианты непересекаемости и покрытия;
- метрики глубины и ширины дерева.

На данный момент файл содержит только намётки и аксиомы, которые будут заменены реальными определениями.
-/

namespace Pnp3

/-- TODO: структура частичного решающего дерева. -/
axiom PDT (n : ℕ) : Type

/-- TODO: листья PDT как список подкубов. -/
axiom leaves {n : ℕ} : PDT n → List (Subcube n)

/-- TODO: глубина PDT. -/
axiom depth {n : ℕ} : PDT n → ℕ

end Pnp3
