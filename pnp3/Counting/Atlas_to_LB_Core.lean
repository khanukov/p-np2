/--
# Перевод атласа в нижние оценки

Файл будет содержать комбинаторные леммы `num_unions`, `covering_power` и `incompatibility`, которые связывают размер словаря SAL с количеством функций, покрываемых с малой ошибкой.

*Текущее состояние*: добавлены только заглушки с подробными комментариями.
-/

namespace Pnp3

/-- TODO: верхняя оценка на количество объединений ≤ k подкубов из словаря размера D. -/
axiom num_unions (D k : ℕ) : ℕ

/-- TODO: лемма об ограничении «покрывающей мощности» словаря с ошибкой ε. -/
axiom covering_power (D k : ℕ) (ε : ℚ) : ℚ

/-- TODO: критерий несовместимости параметров YES/NO. -/
axiom incompatibility : Prop

end Pnp3
