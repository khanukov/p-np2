/--
# Подсчёт «лёгких» функций

Файл предназначен для формализации внешнего факта (F-CNT): числа функций,
которые допускают схемы размера ≤ `s`, оценивается сверху как `2^{O(s log s)}`.

*План выполнения*:
- ввести параметры `s`, `n` и определения «функция имеет схему размера ≤ s»;
- сформулировать теорему `count_small_circuits` как аксиому с ссылкой на источник;
- подготовить интерфейс для использования в файлах нижних оценок.

Пока используется заглушка `axiom`, чтобы другие файлы могли ссылаться на теорему.
-/

namespace Pnp3

/-- TODO: формализовать и доказать (или задокументировать как внешний факт) оценку числа функций. -/
axiom count_small_circuits (n s : ℕ) : ℕ

end Pnp3
