/--
# Мост к магнификации

Цель файла — показать, как нижние оценки из `LowerBounds/` и внешние факты из `Facts_Magnification.lean` совместно дают разделения типа `NP ⊄ P/poly`.

*Задачи*:
- оформить основную теорему `bridge_NP_not_Ppoly` в виде цепочки импликаций;
- зафиксировать параметры `N`, `ε` и указать, где именно используются импортированные факты.

Пока включена только заглушка теоремы.
-/

namespace Pnp3

/-- TODO: центральный мост от нижних оценок к магнификации. -/
axiom bridge_NP_not_Ppoly : Prop

end Pnp3
