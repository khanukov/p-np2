# Worker Prompt D0 — general iso-strong no-go

Question: does the canonical conclusion-side diagonalisation (`isoStrong_conclusion_negative_for_canonical`) scale from the minimal canonical spec to a general `IsoStrongFamilyEventually` family, or is it canonical-only?

Constraints:
- Markdown-only for D0.
- No Lean/source edits.
- Read required files listed by task owner.
- Run `./scripts/check.sh`.
- Produce exactly one report under `reports/`.
