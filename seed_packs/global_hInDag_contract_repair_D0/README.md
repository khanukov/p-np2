# global_hInDag_contract_repair_D0

Seed pack for repairing the `hInDag` contract on asymptotic DAG-side routes so that route hypotheses require a **global** `PpolyDAG` witness (single polynomial-size family), not per-slice hardwiring witnesses.

Deliverable: one markdown report in `reports/`.
