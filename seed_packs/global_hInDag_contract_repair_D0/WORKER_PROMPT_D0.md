# Worker Prompt D0

Task: design a repaired global-`hInDag` contract for asymptotic DAG routes.

Required output: `reports/D0_global_hInDag_contract_repair_<HANDLE>.md` with the ten required sections and explicit hardwiring self-test.
