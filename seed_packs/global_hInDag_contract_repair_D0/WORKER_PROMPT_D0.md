# Worker Prompt (D0)

Task id: `global_hInDag_contract_repair_D0`

Status: skeleton-only bootstrap.

Reason:
- The seed-pack directory was missing.
- Worker constraints require creating only the scaffold files when the skeleton is absent.

Expected next step:
- Land a follow-up markdown-only report under `reports/` once the scaffold exists.
