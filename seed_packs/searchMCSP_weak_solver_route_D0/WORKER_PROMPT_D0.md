# Worker prompt — SearchMCSP weak solver route D0

Branch: `main`

Task type: Markdown-only. Do not write Lean code.

Goal:
Create
`seed_packs/searchMCSP_weak_solver_route_D0/reports/D0_searchMCSP_weak_solver_route_<HANDLE>.md`

Required reading and required sections are defined by the operator task message.

Constraints:

- No Lean code.
- No JSONL edits.
- No NoGoLog edits.
- No spec/trust-root edits.
- No `ResearchGapWitness` construction.
- No `VerifiedNPDAGLowerBoundSource` construction.
- No `SourceTheorem` / `gap_from` / endpoint claims.
- No `P≠NP` claim.

Required command:

- `./scripts/check.sh`
